version: "3"

services:
  server:
    build:
      context: ./server
    volumes:
      - ./logs:/home/node/app/logs
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db/pckd
      - DATABASE_TYPE=postgres
      - JWT_SECRET=supersecretkey123
      - IPREGISTRY_API_KEY=f1ntkcjoqaazglj7
      # إعدادات الروابط
      - CLIENT_URL=https://link.elliaa.com
      - SERVER_URL=https://link.elliaa.com/graphql
      - DOMAIN=link.elliaa.com
      # 👇 هذا السطر هو حل مشكلة 502 👇
      # يجبر السيرفر على قبول الاتصالات من خارج الحاوية (من Nginx)
      - HOST=0.0.0.0
      - PORT=4000
    depends_on:
      - db
    networks:
      - dokploy-network
      - default

  frontend:
    build:
      context: ./client
    # لا نحتاج لفتح منافذ هنا، Dokploy سيوجه للدومين مباشرة
    environment:
      - BACKEND_URL=http://server:4000
    networks:
      - dokploy-network
      - default

  db:
    image: postgres:13-alpine
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pckd
    networks:
      - default

networks:
  dokploy-network:
    external: true
